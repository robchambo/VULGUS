import 'package:flutter/material.dart';

class DemoPuzzleScreen extends StatelessWidget {
  const DemoPuzzleScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Demo')));
}
