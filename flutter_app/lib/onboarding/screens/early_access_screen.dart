import 'package:flutter/material.dart';

class EarlyAccessScreen extends StatelessWidget {
  const EarlyAccessScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Early access')));
}
