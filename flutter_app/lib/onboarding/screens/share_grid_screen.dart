import 'package:flutter/material.dart';

class ShareGridScreen extends StatelessWidget {
  const ShareGridScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Share')));
}
