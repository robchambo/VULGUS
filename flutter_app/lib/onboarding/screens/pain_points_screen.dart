import 'package:flutter/material.dart';

class PainPointsScreen extends StatelessWidget {
  const PainPointsScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Pain')));
}
